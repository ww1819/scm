-- 【已废弃独立执行】合单配送 DDL 已并入：
--   scm-admin/src/main/resources/sql/mysql/scm/{table,column,menu}.sql
-- 见该目录 README.md。本文件仅作历史占位。
/
SELECT 'scm_combined_delivery.sql redirected to classpath:sql/mysql/scm/' AS info;
/
