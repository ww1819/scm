{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "maven-compile-scm",
            "type": "shell",
            "command": "mvn",
            "args": ["compile", "-q"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "problemMatcher": []
        }
    ]
}
