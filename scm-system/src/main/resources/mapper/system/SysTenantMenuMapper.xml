<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scm.system.mapper.ScmTenantMenuMapper">
    <select id="selectMenuIdsByTenantId" resultType="long">
        select menu_id from scm_tenant_menu where tenant_id = #{tenantId}
    </select>
    <delete id="deleteByTenantId">
        delete from scm_tenant_menu where tenant_id = #{tenantId}
    </delete>
    <insert id="insert" parameterType="com.scm.system.domain.ScmTenantMenu">
        insert into scm_tenant_menu (id, tenant_id, menu_id, create_by, create_time)
        values (#{id}, #{tenantId}, #{menuId}, #{createBy}, #{createTime})
    </insert>
</mapper>
