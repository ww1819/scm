<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scm.system.mapper.MaterialCategoryMapper">
    
    <resultMap type="MaterialCategory" id="MaterialCategoryResult">
        <id     property="categoryId"        column="category_id"        />
        <result property="parentId"          column="parent_id"          />
        <result property="ancestors"         column="ancestors"          />
        <result property="categoryCode"       column="category_code"      />
        <result property="categoryName"      column="category_name"      />
        <result property="orderNum"          column="order_num"          />
        <result property="status"            column="status"              />
        <result property="delFlag"           column="del_flag"            />
        <result property="createBy"          column="create_by"           />
        <result property="createTime"        column="create_time"         />
        <result property="updateBy"          column="update_by"           />
        <result property="updateTime"        column="update_time"         />
        <result property="remark"            column="remark"              />
    </resultMap>
    
    <sql id="selectMaterialCategoryVo">
        select category_id, parent_id, ancestors, category_code, category_name, order_num,
               status, del_flag, create_by, create_time, update_by, update_time, remark
        from scm_material_category
    </sql>
    
    <select id="selectMaterialCategoryList" parameterType="MaterialCategory" resultMap="MaterialCategoryResult">
        <include refid="selectMaterialCategoryVo"/>
        <where>
            del_flag = '0'
            <if test="categoryName != null and categoryName != ''">
                AND category_name like concat('%', #{categoryName}, '%')
            </if>
            <if test="categoryCode != null and categoryCode != ''">
                AND category_code like concat('%', #{categoryCode}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="parentId != null">
                AND parent_id = #{parentId}
            </if>
        </where>
        order by parent_id, order_num
    </select>
    
    <select id="selectMaterialCategoryTree" resultMap="MaterialCategoryResult">
        <include refid="selectMaterialCategoryVo"/>
        where del_flag = '0'
        order by parent_id, order_num
    </select>
    
    <select id="selectMaterialCategoryByParentId" parameterType="Long" resultMap="MaterialCategoryResult">
        <include refid="selectMaterialCategoryVo"/>
        where parent_id = #{parentId} and del_flag = '0'
        order by order_num
    </select>
    
    <select id="selectMaterialCategoryById" parameterType="Long" resultMap="MaterialCategoryResult">
        <include refid="selectMaterialCategoryVo"/>
        where category_id = #{categoryId} and del_flag = '0'
    </select>
    
    <insert id="insertMaterialCategory" parameterType="MaterialCategory" useGeneratedKeys="true" keyProperty="categoryId">
        insert into scm_material_category(
            <if test="parentId != null">parent_id,</if>
            <if test="ancestors != null and ancestors != ''">ancestors,</if>
            <if test="categoryCode != null and categoryCode != ''">category_code,</if>
            <if test="categoryName != null and categoryName != ''">category_name,</if>
            <if test="orderNum != null">order_num,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        )values(
            <if test="parentId != null">#{parentId},</if>
            <if test="ancestors != null and ancestors != ''">#{ancestors},</if>
            <if test="categoryCode != null and categoryCode != ''">#{categoryCode},</if>
            <if test="categoryName != null and categoryName != ''">#{categoryName},</if>
            <if test="orderNum != null">#{orderNum},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>
    
    <update id="updateMaterialCategory" parameterType="MaterialCategory">
        update scm_material_category
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="ancestors != null and ancestors != ''">ancestors = #{ancestors},</if>
            <if test="categoryCode != null and categoryCode != ''">category_code = #{categoryCode},</if>
            <if test="categoryName != null and categoryName != ''">category_name = #{categoryName},</if>
            <if test="orderNum != null">order_num = #{orderNum},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null and remark != ''">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where category_id = #{categoryId}
    </update>
    
    <update id="deleteMaterialCategoryById" parameterType="Long">
        update scm_material_category set del_flag = '2' where category_id = #{categoryId}
    </update>
    
    <update id="deleteMaterialCategoryByIds" parameterType="String">
        update scm_material_category set del_flag = '2' where category_id in
        <foreach item="categoryId" collection="array" open="(" separator="," close=")">
            #{categoryId}
        </foreach>
    </update>
</mapper>

