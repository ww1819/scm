<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scm.system.mapper.ScmNoticeReceiverMapper">

    <insert id="batchInsert">
        insert into scm_notice_receiver (id, notice_id, user_id, read_flag, read_time, create_by, create_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.noticeId}, #{item.userId}, #{item.readFlag}, #{item.readTime}, #{item.createBy}, sysdate())
        </foreach>
    </insert>
</mapper>
