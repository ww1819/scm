<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scm.system.mapper.ScmTenantMenuPauseLogMapper">
    <resultMap type="com.scm.system.domain.ScmTenantMenuPauseLog" id="Result">
        <id property="logId" column="log_id"/>
        <result property="pauseId" column="pause_id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="menuId" column="menu_id"/>
        <result property="action" column="action"/>
        <result property="operBy" column="oper_by"/>
        <result property="operTime" column="oper_time"/>
        <result property="remark" column="remark"/>
    </resultMap>
    <select id="selectByTenantId" resultMap="Result">
        select log_id, pause_id, tenant_id, menu_id, action, oper_by, oper_time, remark from scm_tenant_menu_pause_log where tenant_id = #{tenantId} order by oper_time desc
    </select>
    <select id="selectByPauseId" resultMap="Result">
        select log_id, pause_id, tenant_id, menu_id, action, oper_by, oper_time, remark from scm_tenant_menu_pause_log where pause_id = #{pauseId} order by oper_time desc
    </select>
    <insert id="insert" parameterType="com.scm.system.domain.ScmTenantMenuPauseLog">
        insert into scm_tenant_menu_pause_log (log_id, pause_id, tenant_id, menu_id, action, oper_by, oper_time, remark)
        values (#{logId}, #{pauseId}, #{tenantId}, #{menuId}, #{action}, #{operBy}, #{operTime}, #{remark})
    </insert>
</mapper>
